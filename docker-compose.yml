services:
  app:
    build: .
    container_name: seo-monitor
    restart: unless-stopped
    ports:
      - "3100:3000"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
